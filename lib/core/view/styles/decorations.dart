import 'package:flutter/material.dart';

class Decorations {
  static const double mainAppbarHeight = 55.0;
  static const double dashboardPageAppbarHeight = 270.0;
  static const double dashboardPageAppbarRadius = 40.0;
  static const double paddingHorizontal = 16.0;

  static const borderInputRadius = BorderRadius.all(Radius.circular(100));
  static const borderInputMessageRadius = BorderRadius.all(Radius.circular(24));
  static const double widthStrokeInputRadius = 1.0;

  static const cartRadius = BorderRadius.all(Radius.circular(16));

  static const subtitleTextStyle = TextStyle(
    fontWeight: FontWeight.w400,
    fontSize: 10,
  );

  static const buttonShape = RoundedRectangleBorder(borderRadius: BorderRadius.all(Radius.circular(90)));
}
